# Indicator Port Batch 1 — MA Family Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Port the 13 moving-average-family indicators (WMA, HMA, ALMA, DEMA, TEMA, VWMA, LSMA, McGinley Dynamic, Median, TWAP, Envelope, Donchian, Keltner) to the backend registry, each bit-exact against the openalgo-charts TS source via the existing golden gate.

**Architecture:** All ports land in `trading/src/tradex_trading/analytics/indicators.py` following the existing `_builtin_specs()` pattern. Goldens already exist (`trading/tests/analytics/goldens/<id>.json`, generated by `scripts/generate_goldens.mjs`). Each task: extend `PARAM_MAP`/`PLOT_MAP` in `test_golden_parity.py` (red) → port + register (green). The gate is the acceptance test; add ONE edge-case unit test per indicator beyond parity.

**Tech Stack:** Python stdlib only; pytest; existing golden fixtures (~300 bars incl. flat run at bars 140–169, zero-volume bars at 50/51/200).

## Global Constraints

- Reference TS source root: `/Users/apple/Downloads/openalgo-charts-master/src/indicators/` — READ THE ACTUAL SOURCE for every formula before porting. The golden gate (`test_golden_parity.py`) passes only at 1e-9 with None-aligned warmup.
- TS warmup convention: helper arrays are NaN before their window fills; `nulls()` maps NaN→null. Backend equivalent: `None` padding at identical indices.
- Backend params stay snake_case; defaults copied from the TS inputs.
- Registry contract fail-loud: validate positive periods with `ValueError`.
- Existing helpers to reuse (do NOT re-implement): `ema()` (values[0]-seeded, index-0 emit), `sma()`, `atr()`, `_rolling_sma()` (finite-only windows).
- Do NOT modify `scripts/generate_goldens.mjs` or any committed golden JSON.
- Unrelated uncommitted datalake changes exist in the working tree — never stage them; commit explicit paths only.
- Run tests: `uv run pytest trading/tests/analytics/test_golden_parity.py -q -k "<id>"` per indicator; full analytics suite before each commit.
- Baseline before Batch 1: gate 12 passed / 79 skipped; after Batch 1: 12+N passed / 79−N skipped.

## Verified reference facts (from the built bundle)

| id | TS plot keys | inputs (defaults) | TS source |
|---|---|---|---|
| wma | `ma` | length=20 | `calc.ts::wma` |
| hma | `hma` | length=9 | `overlay.ts` HMA calc |
| alma | `alma` | length=9 offset=0.85 sigma=6 | `overlay.ts` ALMA calc |
| dema | `dema` | length=9 | `overlay.ts` DEMA calc |
| tema | `tema` | length=9 | `averages.ts` TEMA calc |
| vwma | `vwma` | length=20 | `averages.ts` VWMA calc |
| lsma | `lsma` | length=25 offset=0 | `adaptive.ts` LSMA calc |
| mcginley-dynamic | `mg` | length=14 | `averages.ts` McGinley calc |
| median | `median,upper,lower,medianEma` | length=3 atrLength=14 atrMult=2 source=hl2 | `averages.ts` MEDIAN calc |
| twap | `twap` | source=ohlc4 offset=0 | `averages.ts` TWAP calc |
| envelope | `upper,basis,lower` | length=20 percent=10 | `overlay.ts` ENVELOPE calc |
| donchian | `upper,basis,lower` | length=20 offset=0 | `overlay.ts` DONCHIAN calc |
| keltner-channel | `upper,basis,lower` | length=20 mult=2 atrlength=10 | `adaptive.ts` KELTNER calc |

---

### Task 1: WMA primitive + HMA

**Files:**
- Modify: `trading/src/tradex_trading/analytics/indicators.py` (add `wma`, `hma` functions + two specs in `_builtin_specs()`)
- Modify: `trading/tests/analytics/test_golden_parity.py` (PARAM_MAP + PLOT_MAP entries)
- Test: `trading/tests/analytics/test_indicator_registry.py` (one edge test)

**Interfaces:**
- Produces: `wma(values: list, period: int) -> list[float | None]` (None before index period−1; linear weights, most recent = period; denom = period*(period+1)/2) — later batches (Coppock, HMA variants) reuse it.
- Produces: `hma(values: list, period: int) -> list[float | None]`; registered ids `wma`, `hma`.

- [ ] **Step 1: Read TS sources** — `calc.ts::wma` (lines ~38–50) and the HMA descriptor calc in `src/indicators/overlay.ts`. Note HMA's exact nesting and where NaN propagates (HMA = wma over `2*wma(n/2) − wma(n)` with period `round(sqrt(n))`; confirm round vs int from source; confirm how half/int periods round for n=9 → n/2=4.5 — match source exactly).

- [ ] **Step 2: Red** — add to PARAM_MAP: `"wma": {"length": "period"}, "hma": {"length": "period"}`; PLOT_MAP identity fallback already handles single-plot ids whose golden key equals backend key — BUT backend plots use `"value"` for sma/ema style. Decide per source: keep backend plot key `"value"` and map PLOT_MAP `"wma": {"value": "ma"}, "hma": {"value": "hma"}`. Run:

```bash
uv run pytest trading/tests/analytics/test_golden_parity.py -k "wma or hma" -q
```
Expected: FAIL (unknown indicator).

- [ ] **Step 3: Implement + register** — follow the `_fn_sma` wrapper pattern:

```python
def _fn_wma(candles, period):
    return wma(closes_only(candles), int(period))

def _fn_hma(candles, period):
    return hma(closes_only(candles), int(period))
```

```python
        IndicatorSpec(
            id="wma", name="WMA", category="Trend", placement="overlay",
            params=(("period", "int", 20),),
            plots=(("value", "line", "WMA"),),
            fn=_fn_wma,
        ),
        IndicatorSpec(
            id="hma", name="HMA", category="Trend", placement="overlay",
            params=(("period", "int", 9),),
            plots=(("value", "line", "HMA"),),
            fn=_fn_hma,
        ),
```

Add both to `__all__`. Validate `period <= 0 → ValueError`.

- [ ] **Step 4: Edge test** — in `TestGoldenValues`: `wma([10,11,12,13], 2)[2] == (11*1+12*2)/3` (weights: oldest×1, newest×2); None positions `[0]`.

- [ ] **Step 5: Green + commit**

```bash
uv run pytest trading/tests/analytics/ -q   # expect all pass, gate +2
git add trading/src/tradex_trading/analytics/indicators.py trading/tests/analytics/
git commit -m "feat: WMA + HMA ports, TS-parity"
```

---

### Task 2: EMA-chained family — DEMA, TEMA, ALMA

**Files:** same pattern as Task 1 (functions + specs + maps + one edge test).

**Interfaces:**
- Produces: `dema(values, period) -> list[float | None]`, `tema(values, period) -> list[float | None]`, `alma(values, period, offset=0.85, sigma=6) -> list[float | None]`; registered ids `dema`, `tema`, `alma`.

- [ ] **Step 1: Read TS sources** — DEMA/ALMA in `src/indicators/overlay.ts`, TEMA in `src/indicators/averages.ts`. Key facts to confirm: DEMA = 2·e1 − e2 where e2 = ema(e1); first print at 2L−2. TEMA = 3(e1−e2)+e3, first print 3L−3. ALMA: gaussian weights `exp(-((i - m)^2)/(2 s^2))` with `m = (offset)*(length-1)`, `s = length/sigma`, normalized; window from period−1.

- [ ] **Step 2: Red** — PARAM_MAP: `"dema": {"length": "period"}, "tema": {"length": "period"}, "alma": {"length": "period", "offset": "offset", "sigma": "sigma"}`; PLOT_MAP: `{"value": "dema"}` / `{"value": "tema"}` / `{"value": "alma"}`. Run `-k "dema or tema or alma"` → FAIL unknown indicator.

- [ ] **Step 3: Implement + register** — reuse `ema()`. Specs: category "Trend", placement "overlay", params `(("period","int",9),)` for dema/tema, `(("period","int",9),("offset","float",0.85),("sigma","float",6.0))` for alma; plots `(("value","line","DEMA"),)` etc. Add to `__all__`.

- [ ] **Step 4: Edge tests** — e.g. `dema` on constant series equals that constant from index 2L−2 onward; `alma` on constant series equals constant from index L−1.

- [ ] **Step 5: Green + commit** (`git commit -m "feat: DEMA, TEMA, ALMA ports, TS-parity"`)

---

### Task 3: Volume/time averages — VWMA, TWAP, McGinley, LSMA

**Files:** same pattern.

**Interfaces:**
- Produces: `vwma(candles, period) -> list[float | None]`, `twap(candles, source="ohlc4") -> list[float | None]`, `mcginley(values, period) -> list[float | None]`, `lsma(values, period) -> list[float | None]`; registered ids `vwma`, `twap`, `mcginley-dynamic`, `lsma`.

- [ ] **Step 1: Read TS sources** — VWMA/TWAP/McGinley/Median in `averages.ts`, LSMA in `adaptive.ts`. Confirm: VWMA = Σ(close·vol)/Σ(vol) rolling; zero-volume window → NaN→None (check source handling). TWAP = running mean of ohlc4 from bar 0 (continuous anchor default) — confirm whether fixture's day boundary resets it (source default anchor). McGinley: seeded from ema then `mg += (src−mg)/(period·(src/mg)^4)`; confirm seed and guard when src==mg. LSMA = least-squares endpoint over window (linreg); confirm offset handling (offset=0 default shifts nothing).

- [ ] **Step 2: Red** — PARAM_MAP entries: `"vwma": {"length": "period"}`, `"twap": {}`, `"mcginley-dynamic": {"length": "period"}`, `"lsma": {"length": "period"}`; PLOT_MAP: `{"value": "vwma"}` / `{"value": "twap"}` / `{"value": "mg"}` / `{"value": "lsma"}`. Run → FAIL.

- [ ] **Step 3: Implement + register.** Specs: vwma/mcginley/lsma overlay `(period,int,…)` defaults 20/14/25; twap overlay params `(())` if source is fixed close-equivalent — CHECK: TS twap source=ohlc4; backend candles expose ohlc — implement ohlc4 directly, no param. Plots `("value","line",…)` except mcginley also `("value","line","McGinley")`. Add to `__all__`.

- [ ] **Step 4: Edge tests** — vwma with zero-volume bar inside window → None at those slots (if source confirms); twap[0] == ohlc4[0].

- [ ] **Step 5: Green + commit** (`git commit -m "feat: VWMA, TWAP, McGinley Dynamic, LSMA ports, TS-parity"`)

---

### Task 4: Band overlays — Envelope, Donchian, Keltner

**Files:** same pattern.

**Interfaces:**
- Produces: `envelope(values, period, percent) -> dict`, `donchian(candles, period, offset) -> dict`, `keltner(candles, period, mult, atr_length) -> dict` — all returning `{"upper","middle"/"basis","lower"}` keyed per below; registered ids `envelope`, `donchian`, `keltner-channel`.

- [ ] **Step 1: Read TS sources** — Envelope/Donchian in `overlay.ts`, Keltner in `adaptive.ts`. Confirm: Envelope basis = SMA(length), bands = basis ± percent% (check exponential flag default false). Donchian = highest/lowest over length, mid = avg, shifted by `offset` bars (default 0 — check shift direction and warmup). Keltner: basis = ema(close,length) (source exp flag default true — confirm), rail = ATR(atrlength) — WHICH atr seeding does TS keltner use (base-bundle Wilder atr vs rma)? Match exactly; bands = basis ± mult·rail. Backend plot keys: use `"middle"` for basis (bollinger precedent) and map in PLOT_MAP.

- [ ] **Step 2: Red** — PARAM_MAP: `"envelope": {"length": "period", "percent": "percent"}`, `"donchian": {"length": "period", "offset": "offset"}`, `"keltner-channel": {"length": "period", "mult": "mult", "atrlength": "atr_length"}`; PLOT_MAP: `{"middle": "basis", "upper": "upper", "lower": "lower"}` for all three. Run → FAIL.

- [ ] **Step 3: Implement + register.** Reuse `sma`, `highest`/`lowest` logic inline (or add tiny private `_highest/_lowest` if Donchian/Keltner need them — check what exists). Specs: all overlay; envelope params `(period 20, percent 10.0)`; donchian `(period 20, offset 0)`; keltner `(period 20, mult 2.0, atr_length 10)`. Plots `(upper/middle/lower, line, …)`. Add to `__all__`.

- [ ] **Step 4: Edge tests** — envelope on flat series → all three equal constant; donchian mid == (hh+ll)/2 at a known index.

- [ ] **Step 5: Green + commit** (`git commit -m "feat: Envelope, Donchian, Keltner ports, TS-parity"`)

---

### Task 5: Median (multi-plot study)

**Files:** same pattern.

**Interfaces:**
- Consumes: `atr()` from Batch 0 fixes.
- Produces: `median_study(candles, length, atr_length, atr_mult) -> dict` with keys `"median","upper","lower","median_ema"`; registered id `median`.

- [ ] **Step 1: Read TS source** — MEDIAN descriptor in `averages.ts`. Confirm: median = nearest-rank percentile 50 of hl2 over `length`; bands = median ± atrMult·ATR(atrLength); medianEma = ema of median (confirm chain + warmup of each plot).

- [ ] **Step 2: Red** — PARAM_MAP: `"median": {"length": "length", "atrLength": "atr_length", "atrMult": "atr_mult"}`; PLOT_MAP: `{"median": "median", "upper": "upper", "lower": "lower", "median_ema": "medianEma"}`. Run → FAIL.

- [ ] **Step 3: Implement + register.** Spec: overlay, params `(length 3, atr_length 14, atr_mult 2.0)`, four plots. Nearest-rank median: sort window, take middle (odd) / lower-middle (even) — MATCH SOURCE'S EXACT RULE. Add to `__all__`.

- [ ] **Step 4: Edge test** — median of known odd/even windows.

- [ ] **Step 5: Green + commit** (`git commit -m "feat: Median study port, TS-parity"`)

---

### Task 6: Batch close-out

**Files:**
- Modify: `trading/tests/analytics/test_golden_parity.py` only if a mapping needs final adjustment

- [ ] **Step 1:** Full suite: `uv run pytest trading/tests/ tests/ -q` — expect prior baseline + Batch 1 additions all green.
- [ ] **Step 2:** Parity summary: `uv run pytest trading/tests/analytics/test_golden_parity.py -q` — expect 12+13 passed / 79−13 skipped.
- [ ] **Step 3:** Commit any residual mapping fixups; update `.superpowers/sdd/progress.md`.
