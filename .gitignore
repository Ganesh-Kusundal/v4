# Credentials — NEVER commit
.env.local
.env
*.env

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Runtime state (root dir only — NOT trading/src/tradex_trading/runtime/)
/runtime/

# Test artifacts
.pytest_cache/
htmlcov/
.coverage

# Tool caches & generated output
**/.mypy_cache/
.ruff_cache/
graphify-out/
.freebuff/
.kilo/
.benchmarks/
.complexipy_cache/
.hypothesis/
.poolside/
.workbuddy-ai/

# Typo duplicate of openalgo-charts/ (never commit)
referances/

# Root Playwright deps (restore via npm ci; package.json + lockfile tracked)
/node_modules/

# OS
.DS_Store
Thumbs.db

# Virtual envs
.venv/
venv/
# superpowers worktrees
.worktrees/

# superpowers scratch (ledger, task briefs)
.superpowers/

# Agent tool state
.commandcode/

# Market data store (281MB+ binary churn; rebuild via backfill_parquet.py)
data/ohlcv/

# Kanban derived evidence (regenerated by kanban.py scan)
.kanban/scan.json

# Frontend: deps and build output are reproducible (npm ci + npm run build);
# only source, package.json and the lockfile are tracked.
frontend/node_modules/
frontend/dist/
# Playwright run artifacts (traces, screenshots, HTML report)
frontend/test-results/
frontend/playwright-report/

# Nested engine clone (independent repo; build via npm ci + npm run build)
openalgo-charts/

# Time-series model experiment scratch
poc/*
!poc/
!poc/README.md
!poc/data/
poc/data/*
!poc/data/build_0950_dataset.py
!poc/rank_0950.py
!poc/timesfm_0950.py

# Runtime state (tokens, instrument dumps; regenerate via broker connect)
trading/runtime/
trading/data/
backups/
data/logs/
data/backfill_2025.json
